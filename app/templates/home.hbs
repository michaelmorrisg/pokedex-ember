<div class="main">
    <div class="body">
        <h1>Pokedex</h1>

        <div class="screen">
            <img src="{{pokemon.sprites.front_default}}" />
            {{poke-screen display=display image=hasImage}}
        </div>
            {{input value=pokename}}
        <button {{action "getPokemon" pokename}}>Go!</button>
        <br/>

        <button class="btn-secondary" {{action "changeComponent" "type" pokemon}}>Type</button>
        <button class="btn-secondary" {{action "changeComponent" "ability" pokemon}}>Abilities</button>
        <button class="btn-secondary" {{action "changeComponent" "sprite" pokemon}}>Shiny</button>
    </div>
</div>
{{outlet}}